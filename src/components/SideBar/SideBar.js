import './sideBar.scss';
import PropTypes from 'prop-types';

const SideBar = ({ setObjectToDisplay }) => {

  return (
    <div>
      
    </div>
  );
};
SideBar.propTypes = {
  setObjectToDisplay: PropTypes.func.isRequired,
};
export default SideBar;
